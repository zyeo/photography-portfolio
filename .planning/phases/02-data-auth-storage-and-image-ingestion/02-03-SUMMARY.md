# Summary 02-03 — Build first image ingestion path

- Added EXIF normalization helpers, authenticated upload route, and a minimal admin upload screen.
- Upload flow stores originals privately and persists nullable metadata fields plus focal-point defaults.
- Commit: `Build first photo ingestion flow`
